model_id: "qwen_local"  # choose: "gemini", "qwen", or "qwen_local"

gemini:
  model: "gemini-2.5-flash"
  temperature: 0.0

qwen_local:
  model: "Qwen/Qwen3-VL-4B-Instruct"  # 3B fits on 8GB RAM; use 7B if you have 16GB+
  device: "auto"                          # "auto", "cpu", "cuda", or "mps" (Apple Silicon)
  temperature: 0.0

qwen: ###### NOT TESTED YET — FOR FUTURE USE ######
  model: "qwen-vl-max"
  base_url: ""   # override via QWEN_BASE_URL env var
  temperature: 0.0

retry:
  max_attempts: 3
  base_delay_seconds: 2.0
  max_delay_seconds: 30.0